{
  "name": "cancelaciones-ai",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm --filter @cancelaciones/web dev",
    "dev:local": "node scripts/dev-local.mjs",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "guard:ci": "node scripts/ci-guards.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "next": "15.5.26",
    "typescript": "^5.8.2",
    "vitest": "^2.1.9"
  }
}
