{
  "name": "@cancelaciones/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "echo \"db: lint handled by typecheck\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cancelaciones/domain": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
